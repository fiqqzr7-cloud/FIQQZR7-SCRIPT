-- FIQQZR7 NOCLIP BYPASS - RED EDITION
-- Boleh tembus part berbayar/restricted
-- Auto reset bila mati, modern UI dengan glow tepi merah
-- TITLE BAR DIPERKECILKAN

local player = game.Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

repeat task.wait() until player and player.Character

-- ==============================================
-- VARIABLES
-- ==============================================
local noclipEnabled = false
local noclipConnection = nil

-- ==============================================
-- NOCLIP FUNCTIONS (BYPASS version)
-- ==============================================
local function noclipOn()
    if noclipConnection then
        noclipConnection:Disconnect()
    end
    
    noclipConnection = RunService.Stepped:Connect(function()
        if not noclipEnabled or not player.Character then return end
        
        -- Loop semua part dalam character
        for _, part in pairs(player.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                -- Force CanCollide = false (bypass)
                part.CanCollide = false
                
                -- Untuk part yang dikunci (berbayar), kita set juga
                -- properties lain yang mungkin block movement
                if part:IsA("MeshPart") or part:IsA("Part") then
                    part.Massless = true  -- Buat part tanpa berat
                end
            end
        end
        
        -- Bypass untuk part dalam workspace yang menghalang
        -- (optional - boleh activate kalau nak tembus semua)
        -- for _, part in pairs(workspace:GetDescendants()) do
        --     if part:IsA("BasePart") and part:IsA("MeshPart") then
        --         part.CanCollide = false
        --     end
        -- end
    end)
    
    print("✅ Noclip BYPASS activated")
end

local function noclipOff()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    
    -- Reset semua part character ke normal
    if player.Character then
        for _, part in pairs(player.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
                if part:IsA("MeshPart") or part:IsA("Part") then
                    part.Massless = false
                end
            end
        end
    end
    
    print("✅ Noclip BYPASS deactivated")
end

-- Auto reset bila character mati
player.CharacterAdded:Connect(function(char)
    -- Tunggu character siap
    task.wait(1)
    
    -- Kalau noclip still on, activate balik
    if noclipEnabled then
        noclipOn()
    end
end)

-- ==============================================
-- CREATE UI MODERN (GLOW TEPI MERAH)
-- ==============================================
local gui = Instance.new("ScreenGui")
gui.Name = "FIQQZR7_Noclip"
gui.Parent = player:FindFirstChild("PlayerGui") or player:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.DisplayOrder = 999

-- Main Container
local container = Instance.new("Frame")
container.Size = UDim2.new(0, 300, 0, 200)
container.Position = UDim2.new(0.5, -150, 0.5, -100)
container.BackgroundTransparency = 1
container.Active = false
container.Draggable = false
container.Parent = gui

-- Main Frame (Hitam kemerahan)
local main = Instance.new("Frame")
main.Size = UDim2.new(1, 0, 1, 0)
main.Position = UDim2.new(0, 0, 0, 0)
main.BackgroundColor3 = Color3.fromRGB(20, 8, 10)  -- Hitam kemerahan
main.BackgroundTransparency = 0
main.BorderColor3 = Color3.fromRGB(200, 50, 50)  -- Merah
main.BorderSizePixel = 1
main.ClipsDescendants = true
main.Active = false
main.Parent = container

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 14)
mainCorner.Parent = main

-- GLOW TEPI MERAH (3 lapis)
local glow1 = Instance.new("UIStroke")
glow1.Color = Color3.fromRGB(150, 0, 0)  -- Merah gelap
glow1.Thickness = 4
glow1.Transparency = 0.7
glow1.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
glow1.Parent = main

local glow2 = Instance.new("UIStroke")
glow2.Color = Color3.fromRGB(200, 50, 50)  -- Merah terang
glow2.Thickness = 2.5
glow2.Transparency = 0.6
glow2.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
glow2.Parent = main

local glow3 = Instance.new("UIStroke")
glow3.Color = Color3.fromRGB(255, 100, 100)  -- Merah muda
glow3.Thickness = 1
glow3.Transparency = 0.5
glow3.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
glow3.Parent = main

-- Animasi pulsing merah
spawn(function()
    while true do
        for i = 1, 40 do
            local t = i / 40
            glow1.Transparency = 0.7 + (math.sin(t * math.pi) * 0.15)
            glow2.Transparency = 0.6 + (math.sin(t * math.pi) * 0.15)
            glow3.Transparency = 0.5 + (math.sin(t * math.pi) * 0.15)
            task.wait(0.02)
        end
    end
end)

-- ==============================================
-- TITLE BAR (KECIK 35px - BOLEH DRAG)
-- ==============================================
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, -20, 0, 35)  -- Dikurangkan dari 60 ke 35
titleBar.Position = UDim2.new(0, 10, 0, 6)  -- Adjusted position
titleBar.BackgroundColor3 = Color3.fromRGB(25, 10, 12)  -- Merah gelap
titleBar.BackgroundTransparency = 0
titleBar.BorderColor3 = Color3.fromRGB(200, 50, 50)  -- Merah
titleBar.BorderSizePixel = 1
titleBar.ZIndex = 10
titleBar.Active = true
titleBar.Parent = main

local titleBarCorner = Instance.new("UICorner")
titleBarCorner.CornerRadius = UDim.new(0, 8)  -- Kurang bulat
titleBarCorner.Parent = titleBar

-- Glow title bar merah
local titleGlow = Instance.new("UIStroke")
titleGlow.Color = Color3.fromRGB(200, 50, 50)
titleGlow.Thickness = 1.5
titleGlow.Transparency = 0.4
titleGlow.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
titleGlow.Parent = titleBar

-- DRAG FUNCTIONALITY
local dragging = false
local dragInput
local dragStart
local startPos

local function updateDrag(input)
    local delta = input.Position - dragStart
    container.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = container.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

titleBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        updateDrag(input)
    end
end)

-- Icon (lebih kecil)
local icon = Instance.new("TextLabel")
icon.Size = UDim2.new(0, 30, 1, 0)  -- Dikurangkan dari 40 ke 30
icon.Position = UDim2.new(0, 6, 0, 0)  -- Adjusted
icon.BackgroundTransparency = 1
icon.Text = "⚡"
icon.TextColor3 = Color3.fromRGB(255, 100, 100)  -- Merah
icon.TextSize = 18  -- Dikurangkan
icon.Font = Enum.Font.GothamBold
icon.ZIndex = 11
icon.Parent = titleBar

-- Title (lebih kecil)
local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.5, 0, 0.5, 0)
title.Position = UDim2.new(0, 40, 0, 5)  -- Adjusted
title.BackgroundTransparency = 1
title.Text = "NOCLIP"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 12  -- Dikurangkan
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.ZIndex = 11
title.Parent = titleBar

-- Subtitle (lebih kecil)
local subtitle = Instance.new("TextLabel")
subtitle.Size = UDim2.new(0.5, 0, 0.3, 0)
subtitle.Position = UDim2.new(0, 40, 0, 20)  -- Adjusted
subtitle.BackgroundTransparency = 1
subtitle.Text = "Bypass"
subtitle.TextColor3 = Color3.fromRGB(255, 150, 150)  -- Merah muda
subtitle.TextSize = 8
subtitle.Font = Enum.Font.Gotham
subtitle.TextXAlignment = Enum.TextXAlignment.Left
subtitle.ZIndex = 11
subtitle.Parent = titleBar

-- Minimize Button (lebih kecil)
local uiVisible = true
local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Size = UDim2.new(0, 18, 0, 18)  -- Dikurangkan dari 22 ke 18
minimizeBtn.Position = UDim2.new(1, -50, 0.5, -9)  -- Adjusted
minimizeBtn.BackgroundColor3 = Color3.fromRGB(40, 20, 22)  -- Merah gelap
minimizeBtn.BorderColor3 = Color3.fromRGB(200, 50, 50)
minimizeBtn.BorderSizePixel = 1
minimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeBtn.Text = "−"
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 14
minimizeBtn.ZIndex = 11
minimizeBtn.Parent = titleBar

local minCorner = Instance.new("UICorner")
minCorner.CornerRadius = UDim.new(0, 4)
minCorner.Parent = minimizeBtn

-- Close Button (lebih kecil)
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 18, 0, 18)  -- Dikurangkan dari 22 ke 18
closeBtn.Position = UDim2.new(1, -28, 0.5, -9)  -- Adjusted
closeBtn.BackgroundColor3 = Color3.fromRGB(120, 30, 30)  -- Merah gelap
closeBtn.BorderColor3 = Color3.fromRGB(200, 50, 50)
closeBtn.BorderSizePixel = 1
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 10
closeBtn.ZIndex = 11
closeBtn.Parent = titleBar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 4)
closeCorner.Parent = closeBtn

-- ==============================================
-- CONTENT FRAME (adjusted)
-- ==============================================
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -20, 1, -55)  -- Adjusted (title bar 35px)
contentFrame.Position = UDim2.new(0, 10, 0, 45)  -- Adjusted
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = main

-- ==============================================
-- STATUS CARD (merah theme)
-- ==============================================
local statusCard = Instance.new("Frame")
statusCard.Size = UDim2.new(1, 0, 0, 70)
statusCard.Position = UDim2.new(0, 0, 0, 0)
statusCard.BackgroundColor3 = Color3.fromRGB(18, 8, 10)  -- Merah gelap
statusCard.BorderColor3 = Color3.fromRGB(200, 50, 50)
statusCard.BorderSizePixel = 1
statusCard.Parent = contentFrame

local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(0, 8)
statusCorner.Parent = statusCard

local statusIcon = Instance.new("TextLabel")
statusIcon.Size = UDim2.new(0, 25, 0, 25)
statusIcon.Position = UDim2.new(0, 8, 0.5, -12)
statusIcon.BackgroundTransparency = 1
statusIcon.Text = "⚡"
statusIcon.TextColor3 = Color3.fromRGB(255, 100, 100)
statusIcon.TextSize = 18
statusIcon.Font = Enum.Font.GothamBold
statusIcon.Parent = statusCard

local statusTitle = Instance.new("TextLabel")
statusTitle.Size = UDim2.new(1, -80, 0, 20)
statusTitle.Position = UDim2.new(0, 38, 0, 15)
statusTitle.BackgroundTransparency = 1
statusTitle.Text = "Noclip Status"
statusTitle.TextColor3 = Color3.fromRGB(220, 220, 220)
statusTitle.TextSize = 11
statusTitle.Font = Enum.Font.Gotham
statusTitle.TextXAlignment = Enum.TextXAlignment.Left
statusTitle.Parent = statusCard

local statusValue = Instance.new("TextLabel")
statusValue.Size = UDim2.new(1, -80, 0, 20)
statusValue.Position = UDim2.new(0, 38, 0, 35)
statusValue.BackgroundTransparency = 1
statusValue.Text = noclipEnabled and "ON" or "OFF"
statusValue.TextColor3 = noclipEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
statusValue.TextSize = 14
statusValue.Font = Enum.Font.GothamBold
statusValue.TextXAlignment = Enum.TextXAlignment.Left
statusValue.Parent = statusCard

-- Toggle Button
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 50, 0, 30)
toggleBtn.Position = UDim2.new(1, -60, 0.5, -15)
toggleBtn.BackgroundColor3 = noclipEnabled and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(40, 20, 22)
toggleBtn.BorderColor3 = Color3.fromRGB(200, 50, 50)
toggleBtn.BorderSizePixel = 1
toggleBtn.Text = noclipEnabled and "ON" or "OFF"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 11
toggleBtn.Parent = statusCard

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 6)
toggleCorner.Parent = toggleBtn

-- ==============================================
-- INFO CARD (merah theme)
-- ==============================================
local infoCard = Instance.new("Frame")
infoCard.Size = UDim2.new(1, 0, 0, 50)
infoCard.Position = UDim2.new(0, 0, 0, 80)
infoCard.BackgroundColor3 = Color3.fromRGB(18, 8, 10)
infoCard.BorderColor3 = Color3.fromRGB(200, 50, 50)
infoCard.BorderSizePixel = 1
infoCard.Parent = contentFrame

local infoCorner = Instance.new("UICorner")
infoCorner.CornerRadius = UDim.new(0, 8)
infoCorner.Parent = infoCard

local infoIcon = Instance.new("TextLabel")
infoIcon.Size = UDim2.new(0, 25, 0, 25)
infoIcon.Position = UDim2.new(0, 8, 0.5, -12)
infoIcon.BackgroundTransparency = 1
infoIcon.Text = "ℹ️"
infoIcon.TextColor3 = Color3.fromRGB(255, 100, 100)
infoIcon.TextSize = 18
infoIcon.Font = Enum.Font.GothamBold
infoIcon.Parent = infoCard

local infoText = Instance.new("TextLabel")
infoText.Size = UDim2.new(1, -40, 1, 0)
infoText.Position = UDim2.new(0, 38, 0, 0)
infoText.BackgroundTransparency = 1
infoText.Text = "Bypass anticheat • Tembus part"
infoText.TextColor3 = Color3.fromRGB(220, 220, 220)
infoText.TextSize = 11
infoText.Font = Enum.Font.Gotham
infoText.TextXAlignment = Enum.TextXAlignment.Left
infoText.Parent = infoCard

-- ==============================================
-- TOGGLE FUNCTION
-- ==============================================
local function toggleNoclip()
    noclipEnabled = not noclipEnabled
    
    statusValue.Text = noclipEnabled and "ON" or "OFF"
    statusValue.TextColor3 = noclipEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
    toggleBtn.Text = noclipEnabled and "ON" or "OFF"
    toggleBtn.BackgroundColor3 = noclipEnabled and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(40, 20, 22)
    
    if noclipEnabled then
        noclipOn()
    else
        noclipOff()
    end
end

toggleBtn.MouseButton1Click:Connect(toggleNoclip)

-- ==============================================
-- MINIMIZE FUNCTION
-- ==============================================
minimizeBtn.MouseButton1Click:Connect(function()
    if uiVisible then
        contentFrame.Visible = false
        main.BorderSizePixel = 0
        main.Size = UDim2.new(0, 300, 0, 55)  -- Adjusted (title bar 35px + padding)
        minimizeBtn.Text = "+"
        uiVisible = false
    else
        contentFrame.Visible = true
        main.BorderSizePixel = 1
        main.Size = UDim2.new(0, 300, 0, 200)
        minimizeBtn.Text = "−"
        uiVisible = true
    end
end)

-- ==============================================
-- CLOSE FUNCTION
-- ==============================================
closeBtn.MouseButton1Click:Connect(function()
    if noclipEnabled then
        noclipOff()
    end
    gui:Destroy()
end)

-- ==============================================
-- TOGGLE WITH INSERT KEY
-- ==============================================
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Insert then
        container.Visible = not container.Visible
    end
end)

-- ==============================================
-- PRINT INFO
-- ==============================================
print("✅ FIQQZR7 NOCLIP BYPASS - RED EDITION")
print("👻 Boleh tembus part berbayar/restricted")
print("📌 Title bar 35px - tak menghalang")
print("📌 Auto reset bila character mati")
print("📌 Tekan Insert untuk toggle UI")
